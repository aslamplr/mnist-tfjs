import * as React from "react";

export default () => (
  <div className="center">
    <span>Type "`" to show/hide the tfjs vis</span>
    <div>
      <button>Load data</button>
    </div>
  </div>
);
